#include <stdio.h>

/**
 * main - main entry
 *
 * Description: this program prints c program with exactly the output
 * Return: always (0);
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");

	return (0);
}
