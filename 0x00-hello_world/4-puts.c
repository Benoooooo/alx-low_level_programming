#include <stdio.h>

/*
 * main - function that prints exactly programming is like multi puzzle,
 *	  followed by new line
 * Return: Always (0)
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");

	return (0);
}
