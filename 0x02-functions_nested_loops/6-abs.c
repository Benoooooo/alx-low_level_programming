#include <stdlib.h>
#include "main.h"

/**
 * _abs - function that computes the absolute value of an integer
 *
 * @c: data type of the absolute number
 * Return: the absolute value of the integer argument, of the appropriate
 * integer type for the function.
 */
int _abs(int c)
{
	return (abs(c));
}
