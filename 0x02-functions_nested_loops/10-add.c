#include "main.h"
#include <stdio.h>

/**
 * add - function that adds two integers and returns the result
 *
 * @c: the first interger
 * @n: the second integer
 *
 * Return: the result of the added number
 */
int add(int c, int n)
{
	return (c + n);
}
