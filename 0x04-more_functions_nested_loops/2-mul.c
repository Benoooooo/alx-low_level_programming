#include "main.h"
/**
 *mul - multipies two integers
 *@a: the first number to multiply
 *@b: the second number
 *Return: the result of multiplication between a param amd b param
*/

int mul(int a, int b)
{
	return (a * b);

}
